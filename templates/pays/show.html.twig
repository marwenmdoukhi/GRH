{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}

<div class=" content-body content-body-mail  ">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb breadcrumb-style2">
            <li class="breadcrumb-item">    <a href="{{ path('pays_index') }}">back to list</a>
            </li>
            <li class="breadcrumb-item active tx-primary " aria-current="page"></li>
        </ol>
    </nav>
    <div class="card card-hover card-task-one ">
        <div class="card-header bg-transparent content-header justify-content-between">
            <h3 class="section-title titshow " style="position:relative; top: 6px;"> {{ pay.titre }}</h3>
        </div>

    <div class="table-responsive   pd-lg-25 ">
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pay.id }}</td>
                <td>Actions</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ pay.titre }}</td>
                <td>
                    <div class="pull-right d-flex">
                        {{ include('pays/_delete_form.html.twig') }}
                        <a href="{{ path('pays_edit', {'id': pay.id}) }}"><i class="far tx-success fa-edit fa-lg"> </i></a>
                    </div>
                 </td>

            </tr>
            </tbody>
        </table>
    </div>

    </div>
</div>




{% endblock %}
