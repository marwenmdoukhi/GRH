{{ form_start(form) }}
<div class=" pd-lg-25">

    <div class="row mt-4">
        <div class="col-md-12">
            <label>Type Congé</label>
            {{ form_widget(form.titre) }}
        </div>

    </div>
    <button class=" mt-4 btn btn-outline-primary">{{ button_label|default('Ajouter') }}</button>

</div>

{{ form_end(form) }}
